const typeTargets=document.querySelectorAll("[data-type]"),typeButtons=document.querySelectorAll("[data-type-open]");typeButtons.forEach(t=>{t.addEventListener("click",e=>{e.preventDefault();const a=t.getAttribute("data-type-open");typeTargets.forEach(t=>{t.removeAttribute("data-visible");const e=t.getAttribute("data-type");a===e&&t.setAttribute("data-visible",!0)})})});